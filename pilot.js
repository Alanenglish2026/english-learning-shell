import {storage} from './storage.js';import {runtime} from './local_runtime.js';
const frame=document.getElementById('blind'),status=document.getElementById('status');let ticket;
window.addEventListener('message',async e=>{if(e.source!==frame.contentWindow||e.origin!=='null')return;try{if(e.data?.type==='ready'){const s=await runtime.request('state'),a=s.attempts.filter(a=>a.mode==='S'&&!a.result&&a.ticket).at(-1);if(!a)throw Error('当前没有等待回应的声音。');const m=await storage.get('media',a.attempt_id);if(!m)throw Error('这段声音已中断，请返回学习。');ticket=a.ticket;frame.contentWindow.postMessage({type:'audio',ticket,blob:m.blob,scene:'饮品台：听英语后递水或茶，不清楚请重说。',actions:['water','tea','repeat','no_action']},'*');}
else if(e.data?.type==='result'&&e.data.ticket===ticket){await runtime.request('listener-return',{ticket,result:e.data.result});frame.contentWindow.postMessage({type:'cleanup'},'*');frame.src='about:blank';status.textContent='已收到回应。';location.href='./lesson.html';}}catch(err){status.textContent=err.message;}});
